# Application Configuration
APP_NAME="Experimentation Platform"
APP_VERSION="0.1.0"
DEBUG=false
SECRET_KEY=your-secret-key-change-in-production

# API Configuration
API_PREFIX=/api/v1
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# Authentication
# Bearer tokens for API authentication (JSON array format)
BEARER_TOKENS=["test-token-1","test-token-2","prod-token-change-me"]

# Database Configuration
DATABASE_URL=postgresql+asyncpg://experiments:password@localhost:5432/experiments
DATABASE_POOL_SIZE=20
DATABASE_POOL_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30
DATABASE_ECHO=false

# Redis Configuration
REDIS_URL=redis://localhost:6379/0
REDIS_POOL_SIZE=10
REDIS_DECODE_RESPONSES=true

# ClickHouse Configuration (for analytics)
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000
CLICKHOUSE_DATABASE=experiments
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Kafka Configuration (for CDC)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC_PREFIX=experiments

# Rate Limiting Configuration
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=60

# Assignment Configuration
ASSIGNMENT_BUCKET_SIZE=10000
ASSIGNMENT_CACHE_TTL=604800  # 7 days in seconds
ASSIGNMENT_DEFAULT_SEED=default-seed-change-in-production

# Event Processing
EVENT_BATCH_SIZE=100
EVENT_BATCH_TIMEOUT=5.0
EVENT_QUEUE_SIZE=1000

# Analytics Configuration
ANALYTICS_CACHE_TTL=60  # 1 minute for results cache
ANALYTICS_MIN_SAMPLE_SIZE=100
ANALYTICS_CONFIDENCE_LEVEL=0.95

# Monitoring and Observability
PROMETHEUS_ENABLED=true
<<<<<<< Current (Your changes)
OPENTELEMETRY_ENABLED=false
=======
PROMETHEUS_PORT=9090
ENABLE_TIMING_MIDDLEWARE=true
SLOW_API_THRESHOLD_MS=1000
LOG_LEVEL=INFO
LOG_FORMAT=json

# Health Check Configuration
HEALTH_CHECK_CACHE_TTL=30
READINESS_CHECK_TIMEOUT=5

# Development Settings
RELOAD=false  # Set to true for development with auto-reload
WORKERS=4  # Number of worker processes (ignored if RELOAD=true)
HOST=0.0.0.0
PORT=8000

# Testing Configuration
TEST_DATABASE_URL=postgresql+asyncpg://test_user:test_pass@localhost:5432/test_experiments
TEST_REDIS_URL=redis://localhost:6379/1
>>>>>>> Incoming (Background Agent changes)
