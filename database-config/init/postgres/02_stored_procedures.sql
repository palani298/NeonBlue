-- Simple stored procedures for experimentation platform
-- This file contains basic stored procedures without complex dependencies

-- Enable required extensions
CREATE EXTENSION IF NOT EXISTS "plpgsql";

-- Basic utility functions (will be created after tables exist via migrations)
-- These are placeholder functions that will be recreated after Alembic migrations
